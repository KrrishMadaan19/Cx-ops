{
  "name": "cx-ops-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute cx-ops-db --remote --file=src/schema.sql"
  },
  "dependencies": {
    "csv-parse": "^5.5.6",
    "hono": "^4.6.3"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
